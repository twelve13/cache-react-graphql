type User {
  id: ID! @id
  name: String!
  current_funds: Int
  source: String
}

type Account {
	id: ID! @id
	name: String!
	goal_amount: Int
	notes: String
	current_amount: Int!
	status: Boolean
}
